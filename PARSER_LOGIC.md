# Parser logic

## Active production parsers

1. SeenThis tag parser
2. Adform tag parser
3. Google Campaign Manager workbook parser

The uploaded file is auto-detected; there is no source selector in the normal workflow.

## ZIP

ZIP parsing is experimental and not exposed in production. A ZIP upload is rejected before parsing/export with a message requesting an official tag export.
